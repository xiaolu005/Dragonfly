

# MySQL 创建表 指定存储引擎 完整写法
## 一、核心语法
建表末尾加：**`ENGINE=引擎名`**
```sql
CREATE TABLE 表名 (
    字段1 类型,
    字段2 类型,
    ...
) ENGINE = 引擎名 DEFAULT CHARSET = utf8mb4;
```

---

## 二、最常用 2 种引擎
### 1. InnoDB（默认、生产必用）
支持事务、行锁、外键、崩溃恢复
```sql
CREATE TABLE user (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50) NOT NULL,
    age INT
) ENGINE = InnoDB DEFAULT CHARSET utf8mb4;
```

### 2. MyISAM
不支持事务、表级锁，查询快，老旧系统用
```sql
CREATE TABLE log_info (
    id INT,
    content TEXT
) ENGINE = MyISAM DEFAULT CHARSET utf8mb4;
```

---

## 三、其他少见引擎
```sql
-- 内存引擎，数据存内存，临时高速表
ENGINE = MEMORY

-- 归档引擎，只插入/查询，不修改删除
ENGINE = ARCHIVE
```

---

## 四、查看当前表存储引擎
```sql
-- 方式1
show create table 表名;

-- 方式2
select table_name,engine 
from information_schema.tables 
where table_schema='你的库名';
```

---

## 五、修改已有表引擎
```sql
ALTER TABLE 表名 ENGINE = InnoDB;
```

---

## 六、补充知识点（面试必问）
1. MySQL **5.5 之后默认引擎就是 InnoDB**
2. 互联网/后端项目**一律 InnoDB**
3. 区别：
- InnoDB：事务ACID、行锁、支持外键、适合增删改查并发
- MyISAM：无事务、表锁、适合静态只读日志类

需要我给你一份 **InnoDB 和 MyISAM 高频面试对比表**吗？



# 什么是幻读（MySQL 面试核心，大白话+专业定义）
## 一、先记住一句话
**幻读**：同一个事务内，**两次范围查询**，中间被别的事务**插入了新数据**，导致第二次查到多出来一行「幻影数据」。

---
## 二、三大隔离问题 快速区分
1. **脏读**：读到别人**未提交**的数据
2. **不可重复读**：同一行，两次查，**内容被改了**（update）
3. **幻读**：范围查询，两次查，**多了/少了一行**（insert/delete）

---
## 三、幻读完整演示（超好理解）
### 前提
- 隔离级别：**可重复读 RR**（MySQL InnoDB 默认）
- 表：`user`，主键 id

### 执行顺序
1. **事务A**
```sql
-- 第一次范围查询：查 id 1~10
select * from user where id between 1 and 10;
-- 结果：只有 2 条数据
```

2. **事务B（并发）**
```sql
-- 插入一条 id=5 的新数据，并提交
insert into user(id,name) values(5,'test');
commit;
```

3. **事务A 再查一次**
```sql
select * from user where id between 1 and 10;
-- 突然多出来一条 id=5 的数据
```
✅ **这就是幻读**：
范围区间内，凭空多出一条「幻影行」，像幻觉一样。

---
## 四、核心本质
- 不可重复读 → 针对**单行修改**（update）
- 幻读 → 针对**范围区间**的**新增/删除**（insert / delete）
- InnoDB 的**行锁**只能锁住已存在的行，**锁不住“空白位置”**，所以别人能插新数据。

---
## 五、怎么解决幻读？
1. **提高隔离级别**
   串行化 `SERIALIZABLE`，完全解决，性能差。
2. **InnoDB 关键：间隙锁 + 临键锁（Next-Key Lock）**
   MySQL 默认 RR 级别下，
   范围查询会自动加 **临键锁**，锁住区间空白位置，**禁止其他事务插入**，
   ✅ **InnoDB 默认 RR 已经解决了幻读问题**。

---
## 六、面试标准背诵版
> 幻读是指在**可重复读**隔离级别下，一个事务执行**范围条件查询**时，
> 其他事务在该范围内**插入或删除数据并提交**，
> 导致当前事务后续相同范围查询，结果集行数不一致，出现幻影数据。
> InnoDB 通过**临键锁**在 RR 级别下解决幻读。

---
需要我顺带把：**间隙锁、临键锁、行锁**三者区别，用最短话术讲清楚吗？





# MySQL 行锁 / 间隙锁 / 临键锁（Next-Key Lock）
极简、面试直接背，区分清楚+解决幻读原理。

## 1. 行锁 Record Lock
- 锁住**已存在的某一行**
- 作用：防止别的事务 `update / delete` 这行
- 缺点：**锁不住空位置**，别人可以在区间里插入新数据
- 场景：精准等值查询（唯一索引、主键）

---

## 2. 间隙锁 Gap Lock
- 锁住**两条数据中间的空白区间**
- 只锁「空隙」，不让别的事务 **insert**
- 不会锁已有数据
- 目的：堵住空白位置，防止插入新数据
- 场景：范围查询、非唯一索引

---

## 3. 临键锁 Next-Key Lock
**= 行锁 + 间隙锁**
InnoDB **RR（可重复读）默认锁算法**
- 既锁住当前存在的行
- 又锁住前后空白间隙
- 区间完全封死：**不能改、不能插、不能删**
  👉 这就是 **MySQL RR 解决幻读** 的核心

---

## 4. 三者对应问题
1. 只加行锁
- 能防：脏读、不可重复读
- 防不住：**幻读**（空隙能插数据）

2. 加 间隙锁
- 专门防：插入新数据 → 解决幻读根源

3. 临键锁
- 全覆盖：行+间隙一起锁
- InnoDB 默认开启

---

## 5. 面试秒杀区分
- **不可重复读**：同一行被修改 → 靠「行锁」解决
- **幻读**：范围里新增/删除 → 靠「间隙锁+临键锁」解决
- RC 隔离级别：**没有间隙锁**，依然存在幻读
- RR 隔离级别：**有临键锁**，杜绝幻读

---

## 6.一句话背诵
- 行锁：锁真实存在的数据行
- 间隙锁：锁数据之间的空白区间，禁止插入
- 临键锁：行锁+间隙锁合体，MySQL 默认RR级别靠它彻底解决幻读。

需要我再给你整理：**MySQL 四大隔离级别 + 各自能解决哪些问题** 对照表吗？





# 硬核结论（面试必背）
**MySQL InnoDB 默认 RR（可重复读）**
✅ **解决了「业务层面的幻读」**
❌ **没有彻底杜绝「理论/狭义幻读」**

---
## 一、先分清两种幻读
### 1. 狭义幻读（数据库理论定义）
事务A 范围查询 → 事务B insert 提交 → A 再查多出数据。
👉 **RR 下：普通快照查询 依然存在**

### 2. 业务幻读（实际开发遇到的）
事务A 先查范围，**再想更新这个区间所有数据**，
发现有一条“看不见但改得到”的幽灵数据，引发业务异常。
👉 **RR 靠 临键锁 彻底解决**

---
## 二、RR 为什么大部分场景没有幻读？
### 1. InnoDB 核心机制：MVCC 多版本并发控制
普通 `SELECT` 是**快照读**
- 事务启动瞬间生成**数据快照**
- 整个事务内，永远读自己的快照
- 别的事务新增、删除，**你完全看不到**
  👉 所以：**两次范围查询，行数永远一样，不会多出数据**

### 2. 加锁语句：当前读
`select ... for update` / `update` / `delete` 属于**当前读**
- 会加 **Next-Key Lock 临键锁（行锁+间隙锁）**
- 锁住整个查询区间+空白缝隙
- 其他事务**无法在区间内 insert**
  👉 从写入源头**堵死幻读产生的条件**

---
## 三、最关键区别（必考）
1. **快照读（普通select）**
     MVCC 隔离新增数据，**看不到幻行**，业务无感。

2. **当前读（加锁/修改）**
     临键锁 + 间隙锁，**禁止插入幻行**，杜绝幻读。

---
## 四、完整版面试标准答案
MySQL InnoDB 在 RR 隔离级别下：
1. 普通查询使用 **MVCC 快照读**，事务内读取固定版本数据，看不到其他事务新增数据，规避了日常业务中的幻读现象；
2. 更新、加锁查询等**当前读**会触发 **临键锁（Next-Key Lock）**，结合行锁与间隙锁，锁定范围区间，防止其他事务插入新数据；
3. 因此**实际业务中 RR 已经解决幻读**，不需要上性能极差的 Serializable 串行化级别；
4. 仅纯理论狭义定义下，快照读仍存在理论幻读，但对开发无影响。

---
## 五、极简背诵版
RR 依靠 **MVCC 快照读** 让普通查询看不到新数据，
依靠 **临键锁+间隙锁** 阻止区间插入，
**工程层面完全解决幻读，是MySQL最优生产隔离级别。**

需要我给你一段「3个事务复现：RC有幻读 / RR无幻读」的SQL演示案例吗？一看就懂。







## 一、什么时候用 `SELECT ... FOR UPDATE`
### 核心场景：**需要「先查再改、强排他、防止并发篡改」**
1. 业务必须保证**查到的数据别人不能改、不能删**
2. 防止超卖、库存扣减、余额变更、订单状态流转
3. 分布式/高并发下，**悲观锁控制**
4. 要锁住某条/某范围数据，不允许其他事务写入

> 普通 `select` 是**快照读、无锁**；
> `select ... for update` 是**当前读、悲观排他锁**。

---

## 二、RR（可重复读）下：普通 `SELECT` 会加行锁吗？
### 结论：**不会**
- 普通不加锁查询：
```sql
select * from t_user where id = 1;
```
✅ 走 **MVCC 快照读**
✅ **不加任何行锁、间隙锁**
✅ 别人照样 update、delete、insert

---

## 三、RR 下 `SELECT ... FOR UPDATE` 才会加锁
### 1. 主键 / 唯一索引 **等值查询**
```sql
select * from t where id = 1 for update;
```
- 只命中**单行存在数据**
- 只加 **Record 行锁**
- 不加间隙锁

### 2. **范围查询 / 非唯一索引 / 查不到数据**
```sql
-- 范围
select * from t where id between 10 and 20 for update;

-- 普通索引等值（非唯一）
select * from t where name='张三' for update;
```
RR 级别会触发：
**Next-Key Lock（临键锁 = 行锁 + 间隙锁）**
- 锁住命中行
- 锁住区间空白间隙
- 其他事务无法在该区间 `insert` → 防幻读

---

## 四、高频面试标准答案
1. `select … for update` 用于**悲观锁场景**，适用于库存、订单、金额等强一致性并发控制，需要锁定查询数据避免并发修改。
2. RR 隔离级别下，**普通 select 是快照读，不加行锁**；
3. 只有 `select … for update`、update、delete 这类**当前读**才会加锁：
   - 唯一索引精准等值：只加**行锁**
   - 范围/普通索引/无命中：加**临键锁（行锁+间隙锁）**

---

## 五、补充区分（秒懂）
- 普通 SELECT：MVCC、无锁、不怕并发
- FOR UPDATE：当前读、排他锁、阻塞别人改/插
- RC 级别：无论什么查询，**都没有间隙锁**，只有行锁






# RR（可重复读）下：普通 SELECT 不会加行锁 那它怎么能重复读呢？ 如果另外一个事务改了这条数据不是不能重复读了吗

这一问问到**MySQL 最核心本质**了，我用**大白话+绝对易懂**给你讲透，一次性彻底打通：
# 核心结论先钉死
1. **RR 普通 SELECT 不加锁**
2. **别人确实可以改这条数据**
3. **但你这边依然能重复读到旧数据**
     👉 靠的不是「锁」，靠的是 **MVCC 多版本快照**

---

# 一、RR 实现「可重复读」的真相
## 1. 普通 SELECT = 快照读（读历史版本）
InnoDB 每一行数据，都会存**多个历史版本**（undo log 日志链）。

- 事务A 开启的一瞬间，会生成一个**读视图（Read View）**
- 事务A 整个生命周期里：
  **永远只看「开启事务那一刻的旧快照数据」**

## 2. 其他事务修改数据发生了什么？
事务B 执行 `update` 修改了这行、并且 commit：
- 物理真实数据**确实被改了**
- 但是：**不会覆盖掉旧版本**
- 旧版本会保留在 undo log 里

## 3. 事务A 再次 select
A 还是拿着自己的老 Read View
👉 自动去找**旧版本数据**返回
✅ 两次查询结果一模一样 = **可重复读**

---

# 二、举个最简例子（一看就懂）
1. 事务A 开启事务
```sql
begin;
select * from user where id=1; 
-- 查到：name=张三（快照固定）
```

2. 事务B 并发修改、提交
```sql
update user set name='李四' where id=1;
commit;
```
👉 数据库**真实数据已经变成李四**

3. 事务A 再查一次
```sql
select * from user where id=1;
```
👉 A 依然读到 **张三**
**因为它读的是 undo log 里的历史快照，不是最新物理数据**

---

# 三、关键区分：靠锁 还是 靠版本？
| 方式                   | 原理             | 是否加锁         |
| -------------------- | -------------- | ------------ |
| **可重复读 RR（MySQL默认）** | **MVCC 多版本快照** | ❌ 不加锁，读写互不阻塞 |
| **串行化 Serializable** | 全表/范围锁         | ✅ 加锁，禁止别人修改  |

### 你疑惑的点完美解答：
> Q：不加锁，别人改了数据，为啥我还能重复读？
> A：
> 别人改的是**最新物理行**，
> 你读的是**事务启动时的历史快照行**，
> 两条数据版本隔离，互不干扰。

---

# 四、那什么时候会读到别人的修改？
只有 **当前读** 才会读最新数据：
- `select ... for update`
- `update`
- `delete`

这类操作：
1. 不走快照
2. 直接读**最新物理数据**
3. 会加行锁/临键锁

---

# 五、面试满分一句话背诵
RR 隔离级别下，普通 SELECT 是**MVCC 快照读**，不加任何行锁；
其他事务可以正常修改最新数据，但当前事务会通过 undo log 读取事务启动时刻的历史数据版本，
从而**不加锁也能实现可重复读**。

---

需要我再给你补一段：
「MVCC、ReadView、undo log 三者简易工作流程」？几分钟彻底吃透。



# 既然select是读的快照里面的内容那为什么会有幻读的问题？

先给你**终极一句话答案**，再层层拆穿，你所有疑惑瞬间打通：

# 核心结论
1. **普通快照读（select 不加锁）**
     ✅ 根本**不会出现幻读**
       你之前理解是对的：读快照，别人新增、修改、删除，你都看不见。

2. **幻读真正出现的场景 = 当前读（update / delete / select for update）**
     ❌ **当前读不走快照，读最新数据**，才会触发幻读。

---

# 一、你现在的矛盾点
你疑问：
> 既然普通 select 都是读快照，看不到别人新增的数据
> 那理论上 RR 根本不可能有幻读，为什么教材还说 RR 有幻读？

✅ 完全正确：
**纯快照读，RR 没有任何幻读。**

❌ 问题出在：**一个事务里 混合了 快照读 + 当前读**

---

# 二、经典「RR 幻读复现案例」（全网最标准）
表：`user` ，主键 id
初始数据：`id=1、id=10`

## 事务A
```sql
begin;
-- ① 普通快照读（查 1~20）
select * from user where id between 1 and 20;
-- 查到：1、10   （快照里没有别的数据）
```

## 事务B 并发执行
```sql
begin;
insert into user(id) values(5);
commit;
```
B 插入成功并提交。

---

## 回到 事务A，执行 **当前读**
```sql
-- ② 当前读：更新区间 1~20
update user set name='test' where id between 1 and 20;
```

👉 重点来了：
1. 刚才**快照读**明明只看到 1、10
2. 但是 **update 是当前读**，直接读磁盘最新数据
3. 直接把 **B 刚插入的 id=5 也更新了**

---

# 三、这就是「幻读」的本质
1. 同一个事务A
- 第一次查询：**明明不存在这条数据**
- 后续更新操作：**却能修改到这条不存在的幽灵数据**

> 看得见的没有，改得着的却有
> 这种**数据不一致**，就叫 **幻读**

---

# 四、彻底分清：两种读取模式
## 1. 快照读（普通 select）
- 走 MVCC、ReadView、undo log
- 只读老版本
- **看不到新插入数据 → 无幻读**

## 2. 当前读（update / delete / for update）
- 不走快照
- 直接读磁盘**最新物理数据**
- 会感知到其他事务 insert / delete
- **会触发幻读**

---

# 五、那 InnoDB RR 到底解决没？
1. **快照读场景**
     ✔ 完全解决幻读（靠MVCC）

2. **当前读场景**
     ✔ 靠 **临键锁+间隙锁**
       锁住整个范围的「数据+空隙」，禁止其他事务 insert
       从而**堵住幻读源头**

---

# 六、总结（背这个面试满分）
1. 普通 `SELECT` 是快照读，基于 MVCC 查询历史版本，**天然不会幻读**。
2. 幻读发生在 **当前读**（更新、删除、加锁查询），会读取数据库最新数据，能感知到其他事务新增的数据。
3. MySQL 的 RR 级别，是通过**间隙锁+临键锁**限制写入，从源头防止当前读场景下的幻读，而非靠MVCC。

---

# 七、一句话秒懂你的疑惑
**幻读不是「查出来多一行」，
而是「查的时候没有，改的时候却有」。**

需要我给你画一张极简流程图：
「快照读 / 当前读 / 幻读 / 临键锁」四者关系图吗？看完永久不会忘。







我给你一个**100%准确、一看就会、面试必过**的判断方法，**不用懂原理，直接看 SQL 关键字就能判断**。

# 终极判断规则（背这个就够了）
## 1. **快照读**
只要是**普通查询**，不加任何锁，就是 **快照读**
```sql
SELECT * FROM table WHERE ...;
```
**特征：**
- 只有 `SELECT`
- 没有 `FOR UPDATE`、`LOCK IN SHARE MODE`
- 不加锁、读历史版本

---

## 2. **当前读**
只要出现下面**任意一种**，就是 **当前读**
```sql
SELECT ... FOR UPDATE;
SELECT ... LOCK IN SHARE MODE;

UPDATE table SET ...;
DELETE FROM table WHERE ...;

INSERT INTO table ...;
```
**特征：**
- 会**读取最新数据**
- 会**加锁**
- 会看到其他事务已提交的新数据（insert/update/delete）

---

# 一句话判断
**一个事务里，既有普通 SELECT，又有 UPDATE/DELETE/SELECT ... FOR UPDATE，
就叫：快照读 + 当前读 混合使用。**

---

# 最经典的「混合场景」（就是幻读发生的场景）
```sql
BEGIN;

-- 1. 普通 SELECT → 快照读
SELECT * FROM user WHERE id BETWEEN 1 AND 10;

-- 2. UPDATE → 当前读
UPDATE user SET name='x' WHERE id BETWEEN 1 AND 10;

COMMIT;
```
✅ 这就是 **快照读 + 当前读 混合**
❌ 这就是 **幻读会出现的地方**

---

# 你现在的疑惑我一次性彻底解答
你问：
> 既然 SELECT 读快照，不会幻读，那为什么还有幻读？

答案：
**因为同一个事务里，后面出现了 UPDATE/DELETE/FOR UPDATE，变成了当前读，
当前读会读最新数据，所以才出现幻读。**

---

# 最简单记忆口诀（3秒判断）
- **只查不改** = 快照读 = 无幻读
- **又查又改** = 快照读 + 当前读 = **会出现幻读**
- **改/删/加锁查询** = 当前读 = 会加锁、会看到最新数据

---

# 最终结论（你之前所有困惑的根源）
**MySQL 的幻读，
不是普通 SELECT 查出来的，
而是 UPDATE / DELETE / SELECT FOR UPDATE 这种当前读“感知”到的。**

---

如果你愿意，我可以给你做一个
**「RR 隔离级别下，快照读 + 当前读 混合导致幻读」的完整可复现 SQL 案例**
你复制到 MySQL 里跑一遍，**这辈子再也不会忘**。







# 结论：**完全正确，100%必须用 当前读**
我给你用极简逻辑+生产场景讲透，彻底闭环你前面所有疑问。

---

## 一、先定死规则
1. **普通快照读（select 不加锁）**
- 读的是**旧快照版本**
- 看不到别的事务最新修改
- **不加任何锁**
- 并发场景下：**一定会超卖、脏覆盖、数据错乱**

2. **当前读（update / delete / select ...for update）**
- 强制读**磁盘最新物理数据**
- 自动加**行级排他锁**
- 并发排队、串行修改
- **保证数据一致性、完整性**

---

## 二、举最经典场景：库存扣减（不用当前读必崩）
商品库存：`stock = 1`

### 错误写法：先用快照读查库存，再扣减
```sql
BEGIN;
-- 快照读，读的是旧数据 stock=1
select stock from goods where id=1; 

-- 并发两个事务都查到库存=1，都满足条件
update goods set stock = stock - 1 where id=1;
COMMIT;
```
后果：
两个请求同时扣减，**本该只剩0，结果变成 -1**
👉 **超卖、数据崩坏**

---

## 三、正确写法：强制「当前读」
### 方式1：查询就加锁（for update）
```sql
BEGIN;
-- 当前读 + 加行锁，锁住这条数据
select stock from goods where id=1 for update;

-- 后续修改安全串行执行
update goods set stock = stock - 1 where id=1;
COMMIT;
```

### 方式2：直接 update（本身就是当前读）
```sql
-- 直接基于最新数据判断+扣减，天然原子+当前读
update goods 
set stock = stock - 1 
where id=1 and stock > 0;
```

---

## 四、核心原理（为什么必须当前读）
并发修改同一条数据时：
1. 你必须**基于别人修改完的最新结果**继续操作
2. 不能基于「老快照旧数据」去计算
3. 并且要**加锁阻塞其他事务**，防止同时修改

👉 这两个要求，**只有当前读能满足**
快照读：既拿不到最新值，又没有锁，完全不适合修改业务。

---

## 五、结合你之前问的「RR & 幻读」串联总结
1. **纯查询、不用改数据**
   用快照读 → 性能高、无锁、够用

2. **但凡要：修改、扣减、状态变更、金额、库存、订单**
   ✅ 必须 **当前读**
   ✅ 必须 走最新数据 + 行锁
   ✅ 避免并发覆盖、数据不一致、幻读业务问题

---

## 六、面试标准背诵
并发修改同一条数据时，普通快照读读取历史版本且无锁，会导致基于旧数据计算，引发数据覆盖与业务异常；
因此**数据更新、强一致性场景必须使用当前读**，强制读取最新数据并加排他锁，保证并发操作串行化，保障数据完整性。

---

需要我给你整理一张：
**快照读 vs 当前读 完整对比表（用途、锁、并发、场景）** 方便你直接背面试吗？





# mysql数据库中字符集utf8和utf8mb4有什么区别



# MySQL 中 utf8 和 utf8mb4 的区别（面试+工作必背）
我给你用**最清晰、最直白、最实用**的方式讲，看完永远不会混淆。

---

# 一、终极结论
1. **MySQL 的 `utf8` 是假的 UTF-8**
   只支持 **最多3字节** 的字符。

2. **MySQL 的 `utf8mb4` 才是真 UTF-8**
   支持 **最多4字节** 的字符。
   **这才是标准 UTF-8！**

3. **现在建库建表必须用：utf8mb4**

---

# 二、具体区别（一眼看懂）
| 特性     | utf8（MySQL假的）      | utf8mb4（真UTF-8） |
| ------ | ------------------ | --------------- |
| 字节长度   | 最多3字节              | 最多4字节           |
| 支持表情符号 | ❌ 不支持（存 Emoji 会报错） | ✅ 完美支持          |
| 支持生僻字  | ❌ 部分不支持            | ✅ 全部支持          |
| 标准性    | 非标准                | **真正标准 UTF-8**  |
| 推荐使用   | 绝对不推荐              | **必须使用**        |

---

# 三、为什么会有假 utf8？
历史遗留问题：
- MySQL 早期做了一个阉割版 UTF-8，只支持3字节
- 后来发现不行，又出了 **utf8mb4**
- **mb4 = most bytes 4**（最多4字节）

---

# 四、最常见报错（你一定遇到过）
```
Incorrect string value: '\xF0\x9F\x98\x82' for column 'name'
```
原因：
**存了 Emoji 表情 😂🔥🌟**
但字段字符集是 **utf8**（3字节存不下4字节表情）

解决：
**改成 utf8mb4** 就好了！

---

# 五、正确建表/建库写法
### 1. 创建数据库
```sql
CREATE DATABASE test
DEFAULT CHARACTER SET utf8mb4
DEFAULT COLLATE utf8mb4_unicode_ci;
```

### 2. 创建表
```sql
CREATE TABLE user (
  name varchar(50)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 3. 修改已有表
```sql
ALTER TABLE 表名 CONVERT TO CHARACTER SET utf8mb4;
```

---

# 六、排序规则（utf8mb4_unicode_ci vs utf8mb4_general_ci）
- **utf8mb4_unicode_ci**：更准确，推荐
- **utf8mb4_general_ci**：更快，精度略差
  👉 **开发统一用：utf8mb4_unicode_ci**

---

# 七、一句话总结（面试必背）
1. MySQL 的 `utf8` 是**阉割版3字节**，不支持表情，已废弃；
2. `utf8mb4` 是**真正4字节标准UTF-8**，支持表情、生僻字，**生产必须用**；
3. 现在所有项目**默认字符集 = utf8mb4**。

---
需要我给你整理 **MySQL 高频面试 15 题终极答案**吗？
包括：事务、锁、MVCC、索引、优化、字符集……



# DEFAULT COLLATE 是干什么用的

# 一、极简大白话
- `CHARSET(字符集)`：**决定怎么存**（文字二进制编码）
- `COLLATE(排序规则/校对集)`：**决定怎么比、怎么排序、怎么对比是否相等**

---
## 1. 核心作用
1. **字符串比较是否区分大小写**
2. **ORDER BY 排序规则**
3. **中文/特殊字符 比对、权重规则**
4. 唯一索引、去重、`where 模糊匹配` 都受它影响

---
## 2. 举个最直观例子
以常用后缀区分：
- `_ci` = **Case Insensitive** 👉 **不区分大小写**
- # `_cs` = Case Sensitive 👉 **区分大小写**

示例：
```sql
-- 校对集带 ci
SELECT 'A' = 'a'; -- 结果：1（相等）
```

---
## 3. MySQL 常用搭配（工作必记）
### 主流标准推荐
```sql
CHARSET = utf8mb4
COLLATE = utf8mb4_unicode_ci
```

### 两种最常用区别
1. **utf8mb4_unicode_ci**
- 基于 Unicode 标准规则
- 比对**更精准**，特殊字符、多国语言兼容好
- **生产推荐、通用首选**

2. **utf8mb4_general_ci**
- 算法简单，**速度快一点**
- 少数特殊字符比对不严谨
- 老项目常用，新项目不推荐

---
## 4. 业务实际影响
### 场景1：账号登录
字段 `collate=xxx_ci`
- `Admin` 和 `admin` 视为同一个
  适合：用户名、邮箱、登录名

### 场景2：验证码、密钥、订单号
需要区分大小写 → 要用带 `_cs` 的校对集
否则：`Abc123` 和 `abc123` 会被判定重复

### 场景3：中文排序
不同 collate 会导致：
- 按拼音排序
- 或按编码值乱序

---
## 5. 完整标准建库语句（记住这个）
```sql
CREATE DATABASE demo
DEFAULT CHARACTER SET utf8mb4
DEFAULT COLLATE utf8mb4_unicode_ci;
```

---
# 一句话面试背诵
`DEFAULT COLLATE` 是**字符串排序与比较规则**，控制大小写敏感、字符比对、排序逻辑；
项目统一使用 `utf8mb4_unicode_ci`，兼顾兼容性与准确性。

# 在mysql中变量的查询和修改方法



# MySQL 变量查询 & 修改 全攻略
MySQL 变量分两大类：
**系统变量**（全局/会话）、**自定义变量**（用户变量）

---

## 一、系统变量（重点）
### 分类
1. **全局变量 global**：整个MySQL服务生效，所有会话
2. **会话变量 session**：只当前连接生效，断开失效

### 1. 查询系统变量
```sql
-- 1. 查单个变量（常用）
show variables like 'autocommit';
show variables like 'transaction_isolation';
show variables like 'character_set%';

-- 2. 查全局变量
show global variables like 'max_connections';

-- 3. 查看所有系统变量
show variables;

-- 4. select 直接查值
select @@autocommit;
select @@global.max_connections;
select @@session.transaction_isolation;
```

### 2. 修改系统变量
#### ① 只改当前会话（临时，断开失效）
```sql
-- 语法：set 变量=值
set autocommit = 1;
set transaction_isolation = 'READ-REPEATABLE';
```

#### ② 改全局（永久生效，重启不丢失需改配置文件）
```sql
set global max_connections = 1000;
set global wait_timeout = 600;
```
> 注意：
> - 加 `global` 对**新连接**生效，当前连接还是旧值
> - 全局变量需要管理员权限

#### ③ 永久修改（重启也生效）
修改 `my.cnf / my.ini` 配置文件，重启MySQL
```ini
[mysqld]
autocommit=1
max_connections=1000
```

---

## 二、自定义用户变量（业务临时用）
### 1. 定义 & 赋值
```sql
-- 方式1：@变量名 无需声明
set @name = '张三';
set @age = 20;

-- 方式2：select into 赋值
select 100 into @num;
```

### 2. 查询自定义变量
```sql
select @name,@age,@num;
```

> 特点：
> - 前缀 `@`
> - 会话级别，断开连接自动销毁
> - 适合SQL脚本临时存值

---

## 三、开发高频实操示例
### 1. 查看/修改事务隔离级别
```sql
-- 查询
select @@transaction_isolation;

-- 修改当前会话为RR
set transaction_isolation = 'REPEATABLE-READ';
```

### 2. 查看/修改自动提交
```sql
show variables like 'autocommit';
set autocommit = 0; -- 关闭自动提交，手动commit
```

### 3. 查看连接超时
```sql
show global variables like 'wait_timeout';
```

---

## 四、关键区别（面试常考）
1. **@@xxx** ：系统变量
2. **@xxx** ：用户自定义变量
3. **session**：当前连接有效
4. **global**：全局所有连接，临时修改重启失效

---

## 五、极简总结
1. 查变量：`show variables like 'xxx'` / `select @@xxx`
2. 改会话变量：`set 变量=值`
3. 改全局变量：`set global 变量=值`
4. 自定义变量：`@变量` 临时使用，连接销毁失效

需要我给你整理「MySQL 常用可改系统变量清单」（隔离级别、超时、连接数、字符集）吗？



# 没有开启事务里变通的查询是快照读还是当前读



先给你**绝对标准答案**，一句话锁死：

# 核心结论
1. **没手动开启事务（默认自动提交 `autocommit=1`）**
   普通 `SELECT` → **依然是 快照读**
2. 只要是**普通不加锁 SELECT**
   不管你有没有手动 `begin`、有没有开启事务
   → **统一都是 快照读（MVCC）**

---

## 一、先分清两个关键
### 1. 快照读（Snapshot Read）
```sql
-- 只要这种普通查询，全是快照读
SELECT * FROM t;
```
特征：
- 走 MVCC、ReadView、undo log
- 读历史版本、**不加锁**
- 不和别人冲突、并发高

### 2. 当前读（Current Read）
```sql
UPDATE / DELETE / INSERT
SELECT ... FOR UPDATE
SELECT ... LOCK IN SHARE MODE
```
特征：
- 读**最新物理数据**
- 加行锁/临键锁
- 阻塞并发、保证强一致

---

## 二、你问的场景：「没有开启事务」
MySQL 默认：
- `autocommit = 1`
- **每条 SQL 自动单独成为一个短事务**

### 执行一条普通 select
```sql
select * from user;
```
底层流程：
1. 自动开启一个**瞬时事务**
2. 生成瞬时 ReadView
3. MVCC 快照读
4. 执行完立刻提交、销毁事务

👉 **所以：还是快照读**

---

## 三、关键误区纠正
❌ 错误理解：
「只有 begin 显式开启事务，才会快照读」

✅ 正确理解：
- **只要是普通 SELECT，一律快照读**
- 不管：手动事务 / 自动事务 / 长事务 / 单条短事务
- **当前读只看 SQL 语法，不看有没有 begin**

---

## 四、一张表彻底背会
| SQL 类型                  | 读类型  | 是否加锁     |
| ----------------------- | ---- | -------- |
| 普通 `SELECT`             | 快照读  | ❌ 无锁     |
| `SELECT ... FOR UPDATE` | 当前读  | ✅ 排他锁    |
| `UPDATE / DELETE`       | 当前读  | ✅ 行锁/临键锁 |

---

## 五、串联你之前所有疑问（终极闭环）
1. 不加锁普通查询 = 快照读 → 靠 MVCC 实现可重复读
2. 改数据/加锁查询 = 当前读 → 靠锁保证并发安全
3. **幻读根源**：同一个业务里，混用「快照读 + 当前读」
4. 并发改数据、扣库存、余额 → **必须用当前读**

---

## 极简背诵版
不在手动事务里、单条普通查询，**仍是快照读**；
快照读与否只看 SQL 是否加锁，**和有没有手动开启事务无关**。



# 排他锁和行锁有什么区别



# 排他锁 和 行锁 区别（极简、面试满分、彻底不混淆）
先记死一句话：
**行锁是「锁的范围」；排他锁是「锁的类型」**
两者不是同级概念，是**交叉关系**。

---

## 一、两个核心定义
### 1. 行锁（Record Lock）
👉 **按「锁住范围」划分**
- 锁的是：**某一行数据**
- 对应：表锁、页锁、元数据锁
- 作用：精准锁定一行，并发高
- InnoDB 优先用**行锁**

### 2. 排他锁（X锁 / Exclusive Lock）
👉 **按「锁的权限/读写类型」划分**
锁分两种：
1. **共享锁 S锁（读锁）**
   大家都能读，不能改
2. **排他锁 X锁（写锁）**
   **我独占**，别人不能读、不能改、不能删

---

## 二、组合关系（最重要）
1. **行锁 分为两种**
- 行级共享锁（行 S 锁）
- **行级排他锁（行 X 锁）**

2. 日常开发说的「加行锁」
     默认指的都是：
       ✅ **行级排他锁**

---

## 三、哪些 SQL 加「行级排他锁」（当前读）
```sql
-- 1. 加锁查询
select * from t where id=1 for update;

-- 2. 修改删除
update t set ... where 条件;
delete from t where 条件;
```

---

## 四、关键区别对比
| 维度   | 行锁          | 排他锁          |
| ---- | ----------- | ------------ |
| 分类维度 | 锁**范围**     | 锁**类型（权限）**  |
| 对立面  | 表锁、页锁       | 共享锁(S锁)      |
| 作用   | 控制锁多少数据     | 控制别人能不能读写    |
| 关系   | 行锁可以是 共享/排他 | 排他锁可以是 行级/表级 |

---

## 五、大白话举例
1. **行锁**
     只锁住 id=1 这一行，别的行随便操作。

2. **排他锁**
     这一行我上锁之后：
- 别人不能改
- 别人不能加锁查
- 完全独占

---

## 六、高频面试标准答案
1. **行锁是锁的粒度概念**，代表只锁定单行数据，并发性能高；
2. **排他锁是锁的类型概念**，属于写锁，独占资源，其他事务不能读写；
3. 我们平时 `update`、`for update` 加的是 **行级排他锁**，既锁单行、又独占互斥。

---

## 七、顺带补：共享锁 vs 排他锁
- 共享锁(S)：读读兼容，读写互斥
- 排他锁(X)：完全互斥，写写、读写都阻塞

需要我再给你讲：**意向锁（IS/IX）是干嘛的**？面试经常连环问。

#  排他锁和行锁分别在什么场景下使用？



我用**最直白、最贴近业务、面试必问**的方式告诉你：
**排他锁 是锁的权限，行锁 是锁的范围，它们是一起用的，不是二选一！**

我直接给你**场景 + 什么时候用 + 对应SQL**，一看就懂。

---

# 一、先给你终极结论（背这个）
1. **行锁 = 锁的范围（只锁一行）**
    目的：**高并发、不影响其他行**
2. **排他锁 = 锁的权限（独占、不让别人改）**
    目的：**并发修改安全、防止超卖、覆盖**
3. **我们 99% 的业务用的是：行级排他锁**

---

# 二、行锁 在什么场景用？
## 行锁 = 只锁某一行，不锁全表
### 使用场景：
**高并发、只修改单条数据、不想影响别人**

比如：
- 修改用户信息
- 扣减单个商品库存
- 更新单个订单状态
- 修改单条记录

### 为什么用行锁？
因为锁范围最小，**并发最高**，别人改别的行完全不影响。

### 触发行锁的SQL（唯一索引/主键）
```sql
-- 行锁（只锁 id=1 这一行）
update user set name='a' where id=1;
select * from user where id=1 for update;
```

---

# 三、排他锁 在什么场景用？
## 排他锁 = 写锁、独占、别人不能读写
### 使用场景：
**必须确保数据安全、不能被并发修改**

比如：
- 扣库存（防止超卖）
- 扣余额（防止多扣）
- 订单状态流转（防止重复操作）
- 任何「先查后改」的并发安全场景

### 为什么用排他锁？
因为**独占、互斥**，同一时间只有一个事务能改。

### 触发排他锁的SQL
```sql
update ...
delete ...
select ... for update
```

---

# 四、最真实、最常用的场景：它们一起用
## 场景：商品库存扣减（高并发）
```sql
begin;
-- 行级排他锁：锁一行 + 独占
select stock from goods where id=1 for update;

-- 扣减
update goods set stock=stock-1 where id=1;
commit;
```

### 这里：
- **行锁**：只锁 id=1，不影响其他商品
- **排他锁**：别人不能改、不能加锁，保证安全

---

# 五、什么时候 不用行锁？
当你要锁**很多行 / 范围**，就会升级成**临键锁/间隙锁**
```sql
select * from user where id > 10 for update;
```
这就不是纯行锁了，会锁区间，防止幻读。

---

# 六、一句话总结（面试满分）
1. **行锁**：需要**高并发、只修改单行、不影响别人**时用
2. **排他锁**：需要**并发安全、防止超卖/覆盖、独占修改**时用
3. **实际业务：99% 都是 行级排他锁 一起用**

---

# 七、你最容易混淆的点我帮你理清
- 行锁 = 锁**多大范围**
- 排他锁 = 锁**什么权限**
- 我们写的 `for update / update`
  **= 行级排他锁**（范围+权限一起生效）

---
需要我给你整理
**「行锁 / 表锁 / 间隙锁 / 排他锁 / 共享锁」使用场景一张图**吗？
面试直接秒杀。

# autocommit=1的时候可以开启事务吗

# 超级清晰答案：**可以！完全可以！**
我用**最直白、最不会错**的方式给你讲透👇

# 一、结论
**autocommit=1（默认自动提交）
不影响你手动开启事务（BEGIN / START TRANSACTION）**

手动开启事务后：
**自动提交会被临时覆盖！**

---

# 二、最关键规则（背这个）
### 1. autocommit=1（默认）
- 每条 SQL 执行完 **自动提交**
- 你不写 `begin`，就是一条条自动提交的短事务
- 你写 `begin`，就**进入手动事务**

### 2. 一旦你写：
```sql
BEGIN;
-- 或者
START TRANSACTION;
```
✅ **autocommit 立刻失效**
✅ 直到你执行 `commit` 或 `rollback` 为止

---

# 三、最直观例子（一看就懂）
## 场景1：autocommit=1，不手动开启事务
```sql
update t set name='a' where id=1;
-- 执行完 → 自动提交了
```

## 场景2：autocommit=1，**手动开启事务**
```sql
BEGIN;          -- 手动开启

update t set name='a' where id=1;
update t set name='b' where id=2;

COMMIT;         -- 你不提交，就永远不提交
```
👉 即使 `autocommit=1`
**只要 BEGIN 一写，就变成手动事务！**

---

# 四、最容易误解的点（90%的人搞错）
❌ 错误理解：
> autocommit=1 = 不能用事务

✅ 正确理解：
> autocommit=1 = **默认每条SQL自动提交**
> 但你依然可以 **手动开启事务**，手动控制提交/回滚

---

# 五、一句话总结（面试必背）
**autocommit=1 只是默认行为，
手动 BEGIN 会覆盖自动提交，
进入显式事务，必须手动 COMMIT / ROLLBACK。**

---

如果你愿意，我可以给你总结
**「autocommit=0 和 autocommit=1 到底有什么区别？」**
1分钟彻底搞懂MySQL事务模式。



# autocommit=0 和 autocommit=1 到底有什么区别？



## 一、先记住核心
`autocommit` 只控制：**单条SQL要不要自动当成一个事务提交**
- `autocommit=1`：**自动提交（默认）**
- `autocommit=0`：**手动提交**

---

## 二、autocommit = 1（MySQL 默认）
1. **不写 BEGIN**
  每条SQL单独是一个独立事务，执行完**立刻自动commit**
```sql
update t set name='1'; -- 执行完直接提交，不可回滚
```
- 普通`select`：仍是**快照读**
- DML（update/delete）：仍是**当前读+行锁**

2. **只要写了 BEGIN / START TRANSACTION**
  👉 自动提交**临时关闭**，进入**手动事务模式**
  必须手动：
```sql
commit;  -- 提交生效
rollback;-- 撤销回滚
```

---

## 三、autocommit = 0（关闭自动提交）
1. **不用写 BEGIN**
  执行第一条DML，**自动开启事务**
  后续所有SQL都在同一个事务里
```sql
update t set name='1';
update t set name='2';
-- 数据暂时没落地，必须手动 commit
commit;
```

2. 断开连接、超时、异常宕机 → 自动 rollback

---

## 四、两者关键区别对照表
| 特性    | autocommit=1（默认） | autocommit=0 |
| ----- | ---------------- | ------------ |
| 单条SQL | 自动提交             | 不会自动提交       |
| 事务开启  | 需手动 `begin`      | 第一条DML自动开启   |
| 回滚能力  | 单条语句无法回滚         | 整段业务可统一回滚    |
| 锁释放   | 语句执行完立刻释放        | commit后才释放锁  |
| 业务常用  | 绝大多数线上项目         | 批量操作、批量导入    |

---

## 五、高频面试必背一句话
1. `autocommit=1` 下依然可以用 `begin` 开启显式事务，手动控制提交与回滚；
2. `autocommit=0` 会隐式开启长事务，不及时 commit 容易造成**长事务、锁等待、脏页堆积**，线上不建议随便开。

---

## 六、补充小知识点（串联你之前所有问题）
1. 日常单条改数据：
  `autocommit=1` + 无`begin` → 短事务、锁秒释放，并发好
2. 库存/金额/跨表多步操作：
  必须 `begin` 开事务 + 最终commit，保证原子性
3. **长事务大忌**：
  `autocommit=0` 忘了commit → 行锁不释放、间隙锁不释放，极易死锁/阻塞






# SELECT ... LOCK IN SHARE MODE的应用场景有哪些



## SELECT ... LOCK IN SHARE MODE 的应用场景
我给你用**最清晰、最实用、面试必背**的方式讲透，全部是真实业务场景。

---

## 一、它是什么（一句话）
`SELECT ... LOCK IN SHARE MODE`
= **加 共享锁（S锁）**
= **可以多人同时读，但所有人都不能修改**

特点：
- 多个事务可以**同时加共享锁**
- 加了共享锁的数据**不能被加排他锁**
- 也就是：**可读不可改**

---

## 二、典型应用场景（真实业务）

### 1. 确保数据在查询期间不被修改
场景：
你需要**连续多次查询同一条数据**，要求两次查询结果完全一致，
但你**不需要修改**，只是为了保证数据稳定。

```sql
BEGIN;
SELECT * FROM user WHERE id=1 LOCK IN SHARE MODE;
-- 中间做一些逻辑计算、校验
SELECT * FROM user WHERE id=1 LOCK IN SHARE MODE;
COMMIT;
```
效果：
- 别人可以读
- **别人不能 update / delete / for update**
- 保证两次查询结果一样

---

### 2. 读校验场景：先确认数据状态，再允许后续操作
场景：
检查订单是否已支付、库存是否足够，**允许多人同时检查**，但**不允许被修改**。

```sql
BEGIN;
SELECT status FROM order WHERE id=100 LOCK IN SHARE MODE;
-- 如果 status=待支付，才继续后续逻辑
COMMIT;
```
作用：
- 防止查询过程中订单状态被悄悄修改
- 多人可以同时查询，不阻塞

---

### 3. 确保数据一致性的“只读事务”
场景：
报表统计、导出数据、对账查询，
要求**数据在统计过程中不能变**，但允许多人同时统计。

共享锁可以保证：
- 统计期间数据不被更新
- 多个统计任务可以并行执行

---

### 4. 防止“不可重复读”，但不想阻塞其他读
场景：
你要保证事务内多次读取一致，
但又不想用 `for update` 把别人全部锁住。

- `for update`：排他锁，我用别人都不能用
- `lock in share mode`：共享锁，大家都能读，都不能改

---

## 三、最关键的对比（面试必考）
| 语句                            | 锁类型    | 允许别人读 | 允许别人写 | 用途           |
| ----------------------------- | ------ | ----- | ----- | ------------ |
| SELECT ... FOR UPDATE         | 排他锁（X） | ✅     | ❌     | 要修改数据、库存、订单  |
| SELECT ... LOCK IN SHARE MODE | 共享锁（S） | ✅     | ❌     | 只查询、不修改、保一致性 |

---

## 四、重要注意事项（必看）
1. **共享锁 + 共享锁 = 兼容**
   多人可以同时加
2. **共享锁 + 排他锁 = 互斥**
   只要有人加共享锁，别人就不能修改
3. **如果先加共享锁，再想修改，会造成死锁风险**
   这是最常见坑
4. **如果你要修改数据，绝对不要用它，要用 FOR UPDATE**

---

## 五、一句话总结（面试满分）
`SELECT ... LOCK IN SHARE MODE` 用于**只读但需要保证数据一致性**的场景，
允许多个事务同时读取，但禁止任何事务修改，
适合查询校验、统计、对账、连续读一致性场景。

---

需要我继续给你讲 **共享锁与排他锁的死锁案例** 吗？

# LOCK TABLES t1 WRITE

## 一、`LOCK TABLES t1 WRITE` 是什么
**整表级 写锁（排他表锁）**，属于MySQL原生表锁语法，**和InnoDB行锁/事务锁不是一套机制**。

### 核心权限
1. 持有锁的当前会话：
- 可以 **读 + 写** t1 表
2. 其他所有会话：
- ❌ 不能查、不能改、不能删、不能增
- 全部**阻塞等待**，直到锁释放

---

## 二、关键语法区别
```sql
-- 加表级写锁
LOCK TABLES t1 WRITE;

-- 释放表锁（必须手动）
UNLOCK TABLES;
```

---

## 三、重要特性（高频考点）
### 1. 脱离事务机制
- `LOCK TABLES` **无视 InnoDB 事务、MVCC、行锁、隔离级别**
- 哪怕 `autocommit=1`、或者在 `begin` 事务里，依然强制上**表锁**
- InnoDB 不推荐用，彻底废掉行并发能力

### 2. 锁范围：整张表
不管条件、索引，直接锁全表，并发直接降级为串行。

### 3. 锁释放时机
- 手动执行 `UNLOCK TABLES`
- 当前会话断开连接自动释放
- **commit/rollback 不会释放表锁**（和事务锁完全不同）

---

## 四、对应另一种：表级读锁
```sql
-- 表级共享读锁
LOCK TABLES t1 READ;
```
- 其他会话：可以读、不能改
- 当前会话：只能读，不能写

---

## 五、适用场景（极少用）
1. 临时**全表强一致性维护**，禁止所有人修改+查询
2. 老旧MyISAM引擎业务（MyISAM没有行锁，只能用表锁）
3. 极端数据迁移、全表批量维护、防止任何并发干扰

---

## 六、和 `for update` 核心区别
| 项    | LOCK TABLES t1 WRITE | SELECT ... FOR UPDATE |
| ---- | -------------------- | --------------------- |
| 锁粒度  | 整张**表**              | 行级/间隙/临键锁             |
| 引擎依赖 | 通杀所有引擎               | 仅InnoDB               |
| 事务关联 | 脱离事务                 | 事务级别，commit释放         |
| 并发性能 | 极差                   | 高                     |
| 隔离级别 | 失效                   | 遵循RR/RC               |

---

## 七、开发禁忌
**InnoDB 生产环境禁止使用 `LOCK TABLES`**
- 会打破MVCC、事务隔离、行锁优势
- 极易造成大面积阻塞、服务雪崩
- 改需求用**行级排他锁 + 事务**代替

---

## 八、极简背诵版
`LOCK TABLES 表名 WRITE` 是**表级排他写锁**，锁住整张表，其他会话全部读写阻塞；
该语法脱离InnoDB事务与行锁机制，仅适配MyISAM，InnoDB项目严禁使用。

# 3阶B树

## 一、3阶B树 核心定义
### 1. 阶数规则
**m阶B树**：
- 每个节点**最多 m 个子节点**
- 节点关键字数量：$\boldsymbol{[\lceil m/2\rceil-1,\ m-1]}$

👉 **3阶B树**（也叫**2-3树**）：
- 最大子节点数：3 个
- 每个节点关键字数量：**最少1个、最多2个**

---

## 二、3阶B树 严格约束
1. 所有**叶子节点在同一层**（平衡，高度一致）
2. 非叶子节点、叶子节点都存「关键字+数据/指针」
3. 节点内关键字**从小到大有序**
4. 关键字划分区间，子树满足：
   - 左子树 < 关键字1
   - 关键字1 < 中子树 < 关键字2
   - 右子树 > 关键字2
5. 分裂规则：
   插入后超过2个关键字 → **中间元素上浮到父节点**，左右拆分

---

## 三、结构形态示例
单个节点合法形态：
- 形态①：`[15]` （1个key，合法）
- 形态②：`[15,30]`（2个key，合法）
- 形态③：`[10,20,35]` → 非法，直接分裂

### 简单三层结构示意
```
        [20]
    /      \
[10]    [30,45]
```
- 根节点：1个key
- 右子节点：2个key，符合3阶规则
- 所有叶子同高度，全局平衡

---

## 四、插入分裂过程（3阶最经典）
插入序列：`10,20,30`
1. 先插入：`[10,20]`（已满，最多2个）
2. 再插入30 → 变成 `[10,20,30]` 超限
3. 中间值**20上浮为根**
4. 分裂为两个子节点：
```
    [20]
   /    \
[10]   [30]
```

---

## 五、3阶B树 特点总结
1. 天然**绝对平衡**，查询效率稳定
2. 只有2种节点：1个key(2分叉)、2个key(3分叉)，所以叫**2-3树**
3. 插入/删除会触发**分裂、合并**，维持平衡
4. 区别B+树：
   - ✅ B树：所有节点都存数据，搜索命中可停在非叶子
   - ✅ B+树：只有叶子存完整数据，非叶子只存索引键

---

## 六、面试一句话背诵
3阶B树又称2-3树，每个节点关键字1~2个、最多3个子节点，全程平衡；插入溢出会中间元素上浮分裂，是理解B树、B+树、数据库索引的基础结构。





# 

## 一、先记死：m 阶 B 树 铁律（通用公式）
设：
- \(m\) = B 树的**阶数**
- \(n\) = 当前节点**关键字个数**
- 分支数（子节点数）= \(n+1\)

### 1. 根节点（特殊，放宽限制）
\[
\boldsymbol{1 \le n \le m-1}
\]
根最少可以 1 个关键字（空树除外）

### 2. 非根 普通节点（严格下限+上限）
\[
\boldsymbol{\lceil \frac{m}{2} \rceil -1 \;\le\; n \;\le\; m-1}
\]
---

## 二、拆成大白话理解
1. **上限（所有节点统一）**
  每个节点**最多关键字**：\(\boldsymbol{m-1}\)
  每个节点**最多子节点**：\(\boldsymbol{m}\)

2. **下限（非根节点）**
  每个节点**最少关键字**：\(\boldsymbol{\lceil \dfrac{m}{2} \rceil -1}\)
  每个节点**最少子节点**：\(\boldsymbol{\lceil \dfrac{m}{2} \rceil}\)

---

## 三、直接套例子，秒懂
### ① 3阶 B 树（m=3）

- 最少关键字：\(\lceil 3/2 \rceil-1 = \boldsymbol{1}\)
- 最多关键字：\(3-1 = \boldsymbol{2}\)
  ✅ 结论：**1～2 个**

### ② 4阶 B 树（m=4，2-3-4树）

- 最少关键字：\(\lceil 4/2 \rceil-1 = \boldsymbol{1}\)
- 最多关键字：\(4-1 = \boldsymbol{3}\)
  ✅ 结论：**1～3 个**

### ③ 5阶 B 树（m=5）

- 最少关键字：\(\lceil 5/2 \rceil-1 = \boldsymbol{2}\)
- 最多关键字：\(5-1 = \boldsymbol{4}\)
  ✅ 结论：**2～4 个**

---

## 四、面试极简背诵
1. 所有节点：关键字**最多 \(m-1\)**
2. 非根节点：关键字**最少 \(\lceil m/2 \rceil-1\)**
3. 子节点数量永远 = 关键字数 + 1
4. 根节点只要求：至少 1 个关键字

---

## 五、快速判断口诀
- 看阶数 m
- 上边界：减 1
- 下边界：先除2向上取整，再减1




## 一、先记死：m 阶 B 树 铁律（通用公式）
设：
- \(m\) = B 树的**阶数**
- \(n\) = 当前节点**关键字个数**
- 分支数（子节点数）= \(n+1\)

### 1. 根节点（特殊，放宽限制）
\[
\boldsymbol{1 \le n \le m-1}
\]
根最少可以 1 个关键字（空树除外）

### 2. 非根 普通节点（严格下限+上限）
\[
\boldsymbol{\lceil \frac{m}{2} \rceil -1 \;\le\; n \;\le\; m-1}
\]
---

## 二、拆成大白话理解
1. **上限（所有节点统一）**
  每个节点**最多关键字**：\(\boldsymbol{m-1}\)
  每个节点**最多子节点**：\(\boldsymbol{m}\)

2. **下限（非根节点）**
  每个节点**最少关键字**：\(\boldsymbol{\lceil \dfrac{m}{2} \rceil -1}\)
  每个节点**最少子节点**：\(\boldsymbol{\lceil \dfrac{m}{2} \rceil}\)

---

## 三、直接套例子，秒懂
### ① 3阶 B 树（m=3）

- 最少关键字：\(\lceil 3/2 \rceil-1 = \boldsymbol{1}\)
- 最多关键字：\(3-1 = \boldsymbol{2}\)
  ✅ 结论：**1～2 个**

### ② 4阶 B 树（m=4，2-3-4树）

- 最少关键字：\(\lceil 4/2 \rceil-1 = \boldsymbol{1}\)
- 最多关键字：\(4-1 = \boldsymbol{3}\)
  ✅ 结论：**1～3 个**

### ③ 5阶 B 树（m=5）

- 最少关键字：\(\lceil 5/2 \rceil-1 = \boldsymbol{2}\)
- 最多关键字：\(5-1 = \boldsymbol{4}\)
  ✅ 结论：**2～4 个**

---

## 四、面试极简背诵
1. 所有节点：关键字**最多 \(m-1\)**
2. 非根节点：关键字**最少 \(\lceil m/2 \rceil-1\)**
3. 子节点数量永远 = 关键字数 + 1
4. 根节点只要求：至少 1 个关键字

---

## 五、快速判断口诀
- 看阶数 m
- 上边界：减 1
- 下边界：先除2向上取整，再减1


# 什么是叶子节点和非叶子节点

## 一、通用定义（所有树：B树、B+树、二叉树 通用）
### 1. 叶子节点（叶节点 / 终端节点）
**没有子节点、没有后代**的节点，树的「最底层末端」。
- 特点：
  - 向下**没有任何分支**
  - 树的最后一层全是叶子

### 2. 非叶子节点（内部节点）
**有子节点、有后代**的节点，用来做索引、分层划分。
- 特点：
  - 下面挂着子节点
  - 包含：根节点、中间所有层级节点

---

## 二、套在你刚学的【B树 / 3阶B树】里
### 3阶B树结构举例
```
        [20]        ← 根节点（非叶子）
      /      \
  [10]      [30,45] ← 这两个是 叶子节点
```
- 非叶子节点：`[20]`
  有左右子树，负责分界、索引导向
- 叶子节点：`[10]`、`[30,45]`
  最底层，没有子节点

---

## 三、结合 MySQL 索引（必考区别）
### 1. 普通 B 树
- 非叶子节点：存索引key + 子节点指针
- 叶子节点：存**完整数据**
- 数据分散在所有层级

### 2. B+树（MySQL InnoDB 用的）
- 非叶子节点：**只存索引键+指针，不存真实行数据**，只用来导航
- 叶子节点：**存放全部完整数据 / 聚簇索引整行数据**
- 所有数据都在叶子层，叶子节点双向链表串联

---

## 四、最简背诵
1. **叶子节点**：最底层、无孩子、末端节点
2. **非叶子节点**：有孩子、管分支、做索引路由
3. B+树核心：非叶子只指路，叶子存真正数据。

